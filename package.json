{
  "name": "music-electrode-balance",
  "version": "1.0.0",
  "private": true,
  "description": "MUSIC Electrode Balance — a single-file browser tool for sizing and balancing Li/Na-ion full cells. The app itself needs no install; this package only provides an optional headless regression test.",
  "scripts": {
    "test": "node tests/smoke.js"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
